services:
  shelfquest:
    build: .
    container_name: shelfquest
    restart: unless-stopped
    ports:
      - "8123:8000"
    environment:
      - LIBRARY_DB=/data/library.db
      - DEFAULT_LOAN_DAYS=7
      - ADMIN_PASSWORD=Letmein!2
    volumes:
      - ./data:/data
